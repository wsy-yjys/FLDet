# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /data01/wsy/UAVDT/ # dataset root dir
train: UAVDT-train/images  # train images (relative to 'path')  23829 images
val: UAVDT-test/images  # val images (relative to 'path')  16580 images
test: UAVDT-test/images  # test images (optional)  16580 images

# Classes
names:
  0: car
  1: truck
  2: bus
